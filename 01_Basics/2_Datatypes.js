"use strict"; // treat all JS code as newer versions


// alert (4-2) we are using node JS, not browser.

console.log(6 +
    1);
// code readability that should be high, avoid this extreme example

console.log("Line 2");


// Datatypes

// Prmitive

// 1. String type

// 2. Number type => 2^53 for bigger number than this we use bigint

// 3. Boolean type => true or false

// 4. null => standalone value

// 5. undefined => value is not assigned

// 6. symbol => used for uniqueness

// Object

console.log(typeof null); // type of null is object

console.log(typeof undefined); // type of undefined is object

